import React from "react"
import { Metadata } from "next"

export const metadata: Metadata = {
    title: "Kani",
    description: "Kani is a platform for creating and sharing Kanban boards",
}

export default function Home() {
    return (
        <div>Home</div>
    )
}
